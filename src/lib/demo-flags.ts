/** Client demo: keep the walkthrough on carta. Flip to true to restore reservas. */
export const SHOW_RESERVATIONS = false

/** Fill analytics / stock / offers so the client sees every panel with data. */
export const USE_DEMO_ANALYTICS = true
